Spicy Sauce: https://github.com/TypeStrong/ts-node/blob/master/README.md
npm install -g ts-node

# Install a TypeScript compiler (requires `typescript` by default).
npm install -g typescript

(added these to package.json)
Steps:
git clone https://github.com/jsynowiec/node-typescript-boilerplate
cd node-typescript-boilerplate
npm install

npm run watch 
cd C:\WebProjects\MyHouseAPI\MyHouse_FirebaseAdmin\build\src
node main.js

edit main.ts to log something to the console - seen when running node main.js 


TODO:
npm install firebase-admin-node
setup proper builds - maybe use webpack or something? 
equally - for now a single file may do (as not really going to do much!)